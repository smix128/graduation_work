{{- $imagePullSecrets := .Values.imagePullSecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: temp-db-{{ .Release.Name }}
data:
  .dockerconfigjson: {{ $imagePullSecrets }}
type: kubernetes.io/dockerconfigjson